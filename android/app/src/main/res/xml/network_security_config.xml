<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
    <!-- If your app needs to connect to other non-HTTPS URLs (typically HTTP),
         you might need to broaden this. However, for just_audio's local proxy,
         127.0.0.1 should be sufficient. Avoid enabling cleartext traffic globally
         if possible for security reasons.
    -->
</network-security-config> 